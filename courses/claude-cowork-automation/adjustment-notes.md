# Adjustment Notes — Claude Cowork: AI Agent Automation

Realism and practicality pass on `proposal-original.md`. Original is unchanged; all edits live in `proposal-adjusted.md`.

## Scorecard

| Check | Verdict |
|-------|---------|
| Hour density | PASS — 4 bullets per hour |
| Objective clarity | PASS — measurable verbs throughout (no "understand"/"be aware of") |
| Day-end lab | PASS — Hour 8 is a hands-on lab |
| Methodology table | PASS — standard six rows retained |
| Prereq match | MINOR — Hour 6 included advanced sub-agent/long-running content for a non-technical, one-day audience |
| **Tooling realism** | **RED FLAG** — per-seat paid Claude licences, managed Macs, connectors, and admin enablement with no concrete lab environment specified |

## Changes by section

- **Section 01:** Added that all practical work uses a synthetic sandbox dataset (never live records), and that Cowork output must always be verified — grounds the data-privacy and capability-maturity risk. Added a delivery note that realised value depends on recurring per-seat licences and an agreed data policy — *tooling realism / cost transparency*.
- **Section 03:** Split into per-participant vs pre-engagement requirements. Specified ISKL-managed Macs with the app **pre-installed**, licences enabled **before Day 1**, connectors **pre-authorised by IT** (admin consent may be required), and a **synthetic-data sandbox** for labs — *closes the tooling-realism red flag and removes lost class time on setup*.
- **Section 04:** Objective 2 reworded from "Set up … install" to "Configure permissions … confirm connection" (install is pre-done). Objective 4 now requires verifying output. Objective 5 now accounts for the device/app availability scheduled tasks need — *capability and scheduled-task realism*.
- **Section 05:** Learning outcomes mirrored to the revised objectives (verify output; understand scheduled-task device dependency).
- **Section 06:** Kept the standard six methodology rows; added a note that labs run on pre-provisioned, Cowork-enabled Macs against a synthetic sandbox, with a recommended 1:8–10 trainer ratio for supervised agent runs — *guided lab environment / tooling realism*.
- **Section 07:**
  - Hour 2 reframed from install-from-zero to a setup check + permission model + connector confirmation — *removes setup time loss*.
  - Hours 4–5 and 8 now explicitly operate on **sample/sandbox data** — *data privacy*.
  - Hour 6 downgraded "long-running tasks and parallel sub-agent work" from a build to a **demonstration/awareness** item, and added what the device/app must have running for scheduled tasks to fire — *prereq match + scheduled-task realism*.
- **Section 10:** Added ISKL-managed Mac with app pre-installed, a synthetic-data sandbox account/folder, and an explicit note that licences are a **recurring per-seat cost** beyond the training — *tooling realism / cost transparency*.
- **Section 11:** Qualified "Works With Existing Google Data" with "subject to administrator-enabled connectors," and added "verify agent output" to the safe-delegation outcome — *connector realism + capability maturity*.
